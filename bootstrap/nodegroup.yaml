apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: basic-cluster2
  region: us-east-1

nodeGroups:
  - name: ng-1
    instanceType: t3.medium
    desiredCapacity: 4
    volumeSize: 80
    iam:
      withAddonPolicies:
        ebs: true
        efs: true
