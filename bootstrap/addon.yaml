apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: basic-cluster2
  region: us-east-1

addons:
  - name: "external-dns"
    useDefaultPodIdentityAssociations: true
  - name: "aws-efs-csi-driver"
    useDefaultPodIdentityAssociations: true
  - name: "vpc-cni"
    useDefaultPodIdentityAssociations: true
