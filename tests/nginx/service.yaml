apiVersion: v1
kind: Service
metadata:
  labels:
    run: nginx
  name: nginx
#  annotations:
#    service.beta.kubernetes.io/aws-load-balancer-scheme: internet-facing
spec:
#  type: LoadBalancer
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    run: nginx
